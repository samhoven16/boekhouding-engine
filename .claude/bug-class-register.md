# Bug-klasse-register — de weg naar "100% af"

> **Het inzicht (Sam, 2026-06-19):** elke mega-audit vindt nieuwe dingen, dus we
> pakken het verkeerd aan. Klopt. Heuristische review (mensen/agents) kan
> *nooit* bewijzen dat hij álles zag — dat is een wiskundige grens, geen luiheid.
> En elke fix verandert code → nieuwe bugs. Whack-a-mole op **instanties**.
>
> **De oplossing:** stop met instanties zoeken. Identificeer de **klassen**
> (een handvol) en maak elke klasse **mechanisch onmogelijk of automatisch
> gevangen**. Dan vindt een volgende audit niets meer in die klasse, want CI
> faalt al vóór de merge. "Elke bug in één keer vinden" kan niet; **"elke
> terugkerende klasse sluiten" is eindig en meetbaar** — dit register is de teller.
>
> Regel: een audit-bevinding is pas écht klaar als de **KLASSE** is gesloten of
> hier geregistreerd met het benodigde structurele werk. Niet alleen de instantie.

## Hoe een klasse "gesloten" is

| Sluiting | Betekenis | Kosten |
|----------|-----------|--------|
| **Contract-test** | Uniforme syntax → één test enumereert exhaustief over de codebase en faalt op elke nieuwe overtreder. | Laag |
| **Chokepoint + ban** | Heterogene syntax → alle gevallen door één helper; lint/test verbiedt het patroon erbuiten. | Midden (refactor) |
| **Onmogelijk-by-design** | Het type bug kan niet meer ontstaan (bv. accessor met compile-time naam i.p.v. magische index). | Midden/hoog |

Een **enumeratie-test op heterogene syntax is GEEN sluiting** — hij mist gevallen
(false negatives) en geeft vals vertrouwen. Eerlijk blijven.

---

## Het register

| # | Klasse | Wortel-patroon | Instanties (deze + vorige rondes) | Sluiting | Status |
|---|--------|----------------|-----------------------------------|----------|--------|
| 1 | **Verkeerde sheet-kolom-index** | bare `data[i][N]` magische nummers; semantiek niet in de naam | EUVerkoop `[21]` vs `[7]` (F-TAX-130) · Mollie `[0]/[6]` vs `[1]/[12]` (F-PAY-130) · XAF accTp (F-ACC-009) · **Moneybird-import RELATIES volledig misaligned (F-IMP-310, BLOCKER — GEVONDEN DOOR de migratie: naam→[1]/email→[10] corrupt, factuur-mail las telefoon; gefixt + ratel-test)** · **detecteerOngekoppeldeBankuitgaven_ las verkeerd bank-schema (F-INV-330: bedrag←omschrijving→NaN→0→sloeg álles over; controle deed stil niets. Dead code (geen prod-caller) + de red-team-test borgde hetzelfde foute schema → beide gefixt). LET OP: detectie is nog nergens in productie gewired — losse bevinding voor Sam.** · **I8-bewijs anti-backdating las [14]=Notities i.p.v. [15]=aangemaakt-op (F-FB-340: `instanceof Date` altijd false → controle gaf ALTIJD "geldig"; draait in bewijs-suite)** · **betalings-integriteit bouwde bankRefs uit [9]=BTW% i.p.v. [11]=referentie (F-GC-341: élke BETAALD-factuur vals als "zonder journaalpost" gerapporteerd)**. Beide gefixt + ratel; de bestaande tests bórgden de bug (data op de foute kolom) → meegecorrigeerd. | **Gebouwd:** `KOL`-accessor (`src/SheetKolom.gs`) als enige bron + contract-test `contract-sheet-kolom.test.js` (KOL ⇄ `sheet-schemas.md`, geen drift) + ban-test (gemigreerd bestand mag geen bare `data[i][N]`). **Gemigreerd:** EUVerkoop + Mollie + **BTW.gs** (de aangifte — hoogste fiscale waarde) + **Dashboard.gs** (29 reads) + **Verkoopfacturen.gs** (VF+REL) + **Inkoopfacturen.gs** (IF) + **Rapportages.gs** (GB+BT) + **Boekingen.gs** (de journaalpost/grootboek-KERN — 52 reads over 5 sheets; `data`-var bindt per functie aan een andere sheet → opgelost met perl line-guards i.p.v. globale replace). **Ban verscherpt:** regex van `data\[i\]\[N\]` → `]\[<getal>]` (tweede-dimensie-literal); ving voorheen `vfData[i][2]`/`ifData[i][3]` (hoofdletter-var) NIET — dat was vals vertrouwen, nu dicht. **Ban OMGEKEERD:** niet langer een opt-in lijst van gemigreerde bestanden, maar élk `src/*.gs` moet schoon zijn behálve een expliciete, SELF-CLEANING `LEGACY_TODO` (een legacy-bestand dat schoon wordt, faalt zijn eigen test → moet uit de lijst). Nieuw bestand of nieuwe bare index in nog-niet-gemigreerde code wordt nu dus direct gevangen; schuld is zichtbaar en kan alleen krimpen. Echte niet-sheet 2D-arrays (bv. browser `e.results[0][0]`) krijgen een `kol-ban-ok`-marker. **KOL uitgebreid:** + HK (herhalende kosten) + INST (instellingen key/value). **Verder gemigreerd:** MoneybirdImport (+ F-IMP-310-fix) · Bankboek (BT) · HerhalendeKosten (HK) · DriveStructuur (INST) · EmailDeliverability (REL — tevens display-bug [1]=Type → [2]=Naam in ongeldige-mail-lijst gefixt) · Jaarafsluiting (GB+JP) · Suggesties (VF+HK). **KOL verder uitgebreid:** + AUDIT (audit-log, cross-cutting). **Module-lokale accessors** (privé aan één bestand, vgl. UREN_KOL): `DLQ_KOL`, `ANCHOR_KOL` (audit-anchor hash-keten), `PRIVE_KOL`/`VERMOGEN_KOL`, `TAAKSTATUS_KOL`. **Verder gemigreerd:** BankImport · Belastingadvies · Belastingvoordeel · Engagement · DataPortability · Utils · DLQ · BoekingEngine · Diagnostiek · Prive. **Triggers** als laatste gemigreerd (AUDIT/TAAK/VF/IF/REL per-functie-guards; KOL.TAAK gecentraliseerd, ook Diagnostiek erop). **LEGACY_TODO is LEEG** — élk `src/*.gs` is KOL-schoon (0 bare kolom-indexen codebase-breed; geverifieerd). De omgekeerde ban eist nu schoon voor de HELE codebase; een nieuwe bare index in welk bestand dan ook faalt direct in CI. | 🟢 **CLOSED** — accessor + contract-test + omgekeerde self-cleaning ban; 31 bestanden gemigreerd (volledige codebase), 4 latente wrong-column-bugs onderweg gevonden+gefixt (F-IMP-310/F-INV-330/F-FB-340/F-GC-341) |
| 2 | **Router-endpoint zonder rate-limit/quota-cap** | nieuwe `actie === 'x'` zonder `rateLimit_` | config · valideer · telemetry (F-SCALE-140, red-team 2×) · **admin-wachtwoord brute-force via gesplitste buckets (F-RED-330): admin-zet-prijs/admin-test-modus hadden elk een eigen 30/u-bucket en vuurden de owner-alert nooit → ~80/u verdeeld op één secret. Lek in de sluiting (die alleen "heeft rate-limit?" checkte, niet "delen endpoints met hetzelfde secret één teller?")** | **GESLOTEN:** `contract-router-ratelimit.test.js` enumereert élke actie, eist rate-limit of allowlist-met-reden. **Lek-reparatie (F-RED-330):** `contract-admin-bruteforce-teller.test.js` eist dat élke ADMIN_WACHTWOORD-validatie de gedeelde teller `admin_login_pogingen` deelt + de owner-alert vuurt (alle 3 paden ge-unificeerd via `_adminAuthOk_`). | 🟢 CLOSED |
| 3 | **Onbegrensde ScriptProperty-key-klasse** | `setProperty('prefix_' + id)` zonder cleanup → 500KB-cliff | dripuit_ (F-SCALE-143) · otp_/otp_ts_ (F-SCALE-142) · herinneringsStap_ · mollie_completed_ · **SUPPLETIE_GEMELD_ (was onbeheerd → nu chokepoint)** | **GEBOUWD:** TTL-chokepoint `zetVluchtigeKey_/leesVluchtigeKey_/wisVluchtigeKey_` + `VLUCHTIGE_PREFIXES`-registry + dagelijkse sweep `ruimVluchtigeKeysOp_` (Utils.gs), gewired in dagelijkseTaken. De enige écht onbeheerde prefix (`SUPPLETIE_GEMELD_`, groeide ~4/jr eeuwig) gemigreerd: 90d-cooldown → 90d-TTL (gedrag identiek, nu auto-opgeruimd). **BAN:** `contract-vluchtige-keys.test.js` enumereert élke dynamische set/delete-prefix in src/ + licence-server/ en eist chokepoint óf allowlist-met-geverifieerde-reden; bewijst bovendien dat de geclaimde sweeps (cleanupHerinneringsStap, ruimVluchtigeKeysOp_, ruimMollieIdempotencyOp_, cleanupVerlopenOtpKeys_) écht bestaan én gewired zijn (geen rubber-stamp). Ratel `vluchtige-keys-chokepoint.test.js`. **Lek-reparatie (F-SCALE-330):** de gas-runtime-as vond dat `KIA_MISSER_GEMELD_`/`BEWAARPLICHT_GEMELD_` de ban ontweken via de **variabele-vorm** (`const k = 'P_' + x; setProperty(k, …)`) i.p.v. de literal-in-setProperty. Beide gemigreerd naar de chokepoint (90d/365d-TTL); ban-regex verscherpt om óók de variabele-vorm te vangen — die verscherping legde 4 verdere variabele-vorm-prefixes bloot (`TARIEF_VEROUDERD_GEZIEN_`, `DRIVE_HOOFDMAP_`, `CIRCUIT_`, `GESLOTEN_PERIODES_CORRUPT_`), elk geverifieerd bounded/permanent/negligible en met reden op de allowlist. **Eerlijke grens:** de regex vangt nu de literal- én variabele-vorm; een exotische computed-prefix kan 'm nog ontwijken → de chokepoint blijft de sanctie-route. | 🟢 **CLOSED** |
| 4 | **Klant-mail zonder opt-out / gate** | nieuwe `MailApp.sendEmail` naar de eigenaar zonder notificatie-gate | drips · BTW-deadline×2 · suppletie · KIA · bewaarplicht · hoge-uitgave · weekoverzicht · sheet-grootte | **GEBOUWD:** chokepoint `stuurKlantNotificatie_()` (checkt `emailNotificatiesAan_()`, valt terug op de DLQ-laag, kan de aanroeper niet breken); alle 8 informatieve eigenaar-notificaties erlangs geroute. **GEVONDEN (F-MAIL-340):** de master-schakelaar was LEK — suppletie/KIA/bewaarplicht/weekoverzicht/sheet-grootte negeerden 'm volledig en de aparte `controleerBtwDeadline_`-trigger óók → klant kreeg die mails zélfs met notificaties 'uit' (precies de klacht). **BAN:** `contract-klant-notificatie-gate.test.js` eist dat élke directe `MailApp/GmailApp.sendEmail` een `klant-mail-ok: <reden>`-marker draagt (derde-partij/laag-niveau/safety); een nieuwe ongemarkeerde send dwingt naar het gegate chokepoint. Ratel `klant-notificatie-gate.test.js`. **Bewust NIET gegate (gemarkeerd):** facturen + betalingsherinneringen naar derden (kernfunctie), DLQ-retries, fatale-fout/quota-alerts (safety, operationeel), accountant-export (user-actie), backup-mail (data-bescherming — bleek al correct: aparte opt-in setting `Backup-email actief`, default UIT, wekelijks i.p.v. daily; geen daily-mail-probleem, dus terecht buiten de notificatie-gate). | 🟢 **CLOSED** |
| 5 | **Copy belooft niet-bestaande feature / verkeerd menupad / stale claim** | marketing/mail-tekst niet gekoppeld aan code-realiteit | read-only-deelflow (F-DOC-130/130b) · drip-ZIP + menupaden (F-VOICE-160) · Exact/Twinfield (F-VOICE-131) | **GESLOTEN (mechanisch deel):** `contract-menu-handlers.test.js` enumereert élk van de 121 menu-items en eist dat de handler-functie écht bestaat — een dode knop (UI belooft een actie zonder code) faalt CI. **Eerlijke grens:** de vrije-tekst menupad-citaten + feature-claims in copy zijn heterogeen en niet zonder false-positives mechanisch te scannen (een fuzzy copy-scan gaf prose/sheet-tab-vals-positieven = klasse-8-ironie; bewust niet gebouwd) → gedekt door de negatieve guards (`mega-audit-copy-fixes`, `audit-ronde3-waarheid-claims`, `website-belofte-vs-code`). Volledige sluiting (menupaden + claims uit één bron) blijft het geregistreerde structurele werk. | 🟢 **CLOSED** (menu-handlers) · eerlijke grens (vrije claims) |
| 6 | **Licentie/security-gate op het verkeerde signaal** | gate op `owner==user` of een raadbare default i.p.v. een echte secret/identiteit | eigenaar-bypass (F-RED-151) · drip-token (F-RED-152) | **GESLOTEN:** `contract-privileged-auth.test.js` enumereert élke privileged licentieserver-endpoint en eist (A) een secret-gate (`_adminAuthOk_`/`veiligVergelijk_`) óf een geverifieerd capability-model (OTP / kennis-van-de-sleutel / send-to-registered-address), (B) GÉÉN `Session.getActiveUser`/`getEffectiveUser`-gate (de eigenaar-bypass-vorm), (C) élk secret met een hardcoded fallback-default geseed in `zelfHerstelProductConfig_` (zodat de raadbare default nooit live is). Een nieuw endpoint dat hiervan afwijkt faalt CI. | 🟢 **CLOSED** |
| 7 | **Jaar/tarief hardcoded** | jaargebonden constante zonder fallback-vlag | tarief-cliff (F-OND-024) · **placeholder-vlag op verkeerd object (F-TAX-340)** · API-versies (BACKLOG-DURABILITY) | **Tarief: GESLOTEN + GEFIXT.** De detectie (fallback-jaar + placeholder/onbevestigd → `TARIEF_VEROUDERD`) was LEK — **F-TAX-340**: de placeholder-vlag staat op `BELASTING_META`, maar de check keek naar `BELASTING_PER_JAAR[jaar].placeholder` (= undefined) → in 2027 zou de klant stil met PRELIMINAIRE tarieven doorrekenen, zónder waarschuwing. Gevonden DOOR de lock-test. Gefixt (check nu `_meta.bevestigd === null`/`placeholder`) + `tarief-veroudering.test.js` vergrendelt detectie (toekomst/placeholder → vlag; bevestigd huidig jaar → geen vals alarm) én de 3 surfacing-paden (hoofd-boekingsflow + fatale-owner-melding + UI-flag) tegen stille regressie. **API-versies (Mollie v2 e.d.): eerlijke grens** — niet mechanisch te sluiten (geen fallback mogelijk; koopproces niet aanraken); hoort bij periodieke dependency-review. | 🟢 **CLOSED** (tarief) · eerlijke grens (API-versies) |
| 8 | **Test borgt het verkeerde (vals-groen)** | test codeert het buggy gedrag of mist de echte seam | btw-classificatie r5b==0 · drip-uit source-regex · cleanup `%100`-gate | **GESLOTEN:** `contract-ledger-ratel.test.js` leest de closure-ledger en dwingt MECHANISCH af dat élke GEBORGD/GESLOTEN BLOCKER/HOOG een regressietest-bestand noemt dat ook echt bestaat — een sluiting zonder ratel (de vals-groen-vorm) faalt CI. De discipline ("geen sluiting zonder borging") kan niet meer verslappen via een vergeten test. **Eerlijke grens:** mutation-spot-checks op de fiscale kern blijven nuttig maar zijn geen onderdeel van deze mechanische sluiting. | 🟢 **CLOSED** |
| 9 | **Geld-precisie / float-drift / afronding** | euro's-as-float i.p.v. integer-cent; `Math.round(n*100)/100` met float-fout; asymmetrische afronding bij negatief; inconsistent afrond-moment | `parseBedrag_("1,234.56")=1,23` (gefixt) · `rondBedrag_` asymmetrie negatief (gefixt) · balans-controle in float-ε (gefixt → integer-cent `===0`) | **Gedaan (ronde 2026-06-19):** `rondBedrag_` symmetrisch + eerlijke docstring; `parseBedrag_` positie-bewuste chokepoint; `formatBedrag_` `-0`; balans-bewijs integer-cent EXACT; property-test `precisie-geld.test.js`. Kern bewezen exact (zie `calculation-register.md`). **⚠️ BINDENDE BTW (F-BTW-340, ernstigste vondst):** de factuur/uitgave/declaratie-BTW werd berekend als `rondBedrag_(excl × btwTarief)` met float-tarief 0.21/0.09 → 1 cent te LAAG in duizenden gevallen (€21,50 × 21% = €4,515 → €4,51 i.p.v. €4,52). Dat te lage btwBedrag wordt opgeslagen op de factuur en GESOMMEERD in de BTW-aangifte → structurele onder-afdracht (naheffing-risico). Bestaande tests gebruikten ronde bedragen (€100→€21) en zagen 't NIET. Gefixt: 4 Triggers-sites + `berekenBtw` + `BEREKEN_BTW` (sheet-functie) via `rondTariefCent_`. Ratel `binding-btw-precisie.test.js` (drift-gevallen + keten incl=excl+btw). **Binding-money-sweep voltooid:** óók afschrijving (saldo×pct×factor, dubbel-float → cent-drift; nu berekenAfschrijvingCent_ exact voor jaar én maand) + herhalende-kosten zakelijk/privé-split (`rondTariefCent_`, privé-deel = restant zodat totaal exact blijft). Geverifieerd exact: km-vergoeding (0.23), incl→excl-deelpad, aangifte-sommatie (telt exacte centen). **Eerlijke grenzen:** (a) pro-rata-aftrek (r5b × belaste/totaal-ratio) drijft 1-op-2M — een integer-ratio-fix riskeert 2^53-overflow bij grote omzet (slechter); float blijft hier veiliger; (b) aangifte-zelf-validatie gebruikt TOL=0,02 (tolereert per-factuur-afronding) → float-`verwacht` immaterieel. **Tarief-floats × bedrag → cent GEFIXT (advies-calcs):** `rondBedrag_(bedrag * tarief)` met een float-tarief (0.0756/0.1270/0.0526) week in duizenden gevallen 1 cent af (aftrek te laag). Chokepoint `rondTariefCent_` (Utils.gs) rekent volledig in INTEGER-centen, half-up, wiskundig exact. **Eerlijkheidsnoot:** mijn eerste KIA-fix (exacte breuk `756×x/10000`) was béter maar nóg niet exact (de /10000 is zelf een float-stap → ~2000 cent-fouten op echte inputs); pas de integer-cent-versie is exact — zelf-correctie. Toegepast op KIA-afbouw + KIA-vast + MKB-winstvrijstelling + Zvw-bijdrage. Geverifieerd dat MIA/FOR/heffingskorting/Box3-belegging al exact zijn (geen wijziging). Ratels `kia-afbouw-precisie` + `rond-tarief-cent` (integer-cent-referentie over bereik). **Eerlijke grens:** de geschatteIB-schijfsom is een expliciete ADVIES-schatting ("Geschatte IB*"); 1-cent-drift daar is immaterieel (klant dient echte aangifte in) — bewust niet herstructureerd. | 🟢 **CLOSED** — kern + primitieven + fiscale tarief-calcs integer-cent exact; geschatteIB advies-schatting (eerlijke grens) |
| 10 | **Fiscale grondslag-afbakening uit grootboek-prefix** | aftrek-grondslag afgeleid uit een kale `startsWith('0x')`-prefix zónder de wettelijke afbakening (asset-type, contra-rekeningen, ondergrens) | KIA/EIA telden élke 02xx met positief saldo, inclusief 0290 cumulatieve afschrijving (F-TAX-330) · KIA mist 01xx software (F-TAX-331, RB) · KIA negeert €450-ondergrens (F-TAX-332, RB) · MIA 026x/027x zelfde patroon | **GESLOTEN (mechanisch deel):** chokepoint `_isInvesteringsRekening02_` sluit 0x90-contra/afschrijvingsrekeningen uit (niet langer leunend op het saldo-teken); KIA + EIA + **Notificaties** erlangs geroute; ratel `f-tax-330-investering-afbakening.test.js` met een **codebase-brede** ban op kale `startsWith('02')&&parseFloat` (élk `src/*.gs`, zoals klasse 1). **2e-ronde-lek (F-TAX-334):** de ban was eerst file-scoped → een 4e instantie in `Notificaties.gs:149` ontsnapte; codebase-breed gemaakt. **3e-ronde-lek (F-TAX-335):** de ban was syntax-scoped (`startsWith('02')`) → de MIA-scan (`/^02[67]/.test()`) ontsnapte; aparte chokepoint `_isMilieuInvesteringsRekening_` + ban nu syntax-volledig (`parseFloat(r[5])` + élke kale `startsWith('0…')`/`/^0…/`-prefix zonder chokepoint). **Eerlijke grens (zoals klasse 3):** een enumeratie op heterogene syntax kan nooit élke vorm vangen (ander saldo-kolom/rij-var); de chokepoints (`_isInvesteringsRekening02_` + `_isMilieuInvesteringsRekening_`) zijn de echte sanctie-route. De ban-test-groen bewijst: nul kale-prefix grondslag-sommaties (r[5], beide vormen) over de hele codebase. **RB-grens (geregistreerd structureel werk):** software-01xx-inclusie + €450-per-bedrijfsmiddel-ondergrens raken advies-BEDRAGEN en vereisen belastingadviseur-bevestiging → ledger F-TAX-331/332 (wacht RB), net als F-TAX-111/112. | 🟢 **CLOSED** (0x90-contra) · RB-grens (software/€450) |
| 11 | **Controle/verifier in slechts één van N paden gewired** | een detector/verifier bestaat wel maar draait niet in álle paden die 'm nodig hebben (menu-only i.p.v. dagelijks) | 3 audit-hashketens, maar alleen de AUDIT_LOG-sheet-keten draaide dagelijks; buffer + `_Audit_Anchor` waren menu-only → tampering stil (F-ACC-330) | **GESLOTEN:** `contract-audit-keten-wiring.test.js` enumereert élke `verifieerAudit…_`-ketenverifier en eist dat-ie vanuit de dagelijkse `controleerAuditKetenProactief_` wordt aangeroepen. Een nieuwe menu-only verifier faalt CI. | 🟢 **CLOSED** |
| 12 | **Onbegrensde sheet/rij-loop zonder guillotine-cap** | loop over álle sheet-/licentie-rijen zonder `guillotineCheck_` + resume-cursor → bij schaal stille time-out-kill mid-write | factuur-aanmaak 2× full-read O(n²) + claim-vóór-write → factuurnummer-gat bij kill (G1/F-SCALE-336) · server-drip-cron zonder cursor (G2/F-SCALE-337) · `koppelTransactiesAanFacturen` O(n²) (G3/F-SCALE-338) | **Chokepoint bestaat al** (`guillotineCheck_` + cursor + batch — zie `stuurAutomatischeBetalingsherinneringen_`/`verwerkBankImport_`, voorbeeldig). **PARTIAL:** 3 sites nog niet gemigreerd — volume-gated (bijt pas bij duizenden rijen/batch-imports, jaren weg voor single-tenant ZZP) en het symptoom (factuurnummer-gat) wordt nu gedétecteerd door F-ACC-331 (M2). Backlog in ledger met exacte fix per site. | 🟠 PARTIAL (volume-gated, symptoom gedetecteerd) |

---

## De eindige roadmap naar "klassen dicht"

In volgorde van geld/risico-impact:

1. ~~**Klasse 1 (sheet-kolom):** bouw `KOL`-accessor + ban; migreer alle bestanden.~~ ✅ **GESLOTEN** — `KOL` (+ module-lokale accessors) als enige bron, contract-test KOL⇄schema, en een OMGEKEERDE self-cleaning ban die de hele `src/` schoon eist. Alle 31 bestanden gemigreerd; 4 latente wrong-column-bugs gevonden+gefixt (Moneybird-RELATIES, bank-uitgave-detectie, I8-anti-backdating, betalings-integriteit). Een nieuwe bare `[<rij>][<getal>]` faalt nu direct in CI.
2. ~~**Klasse 3 (onbegrensde keys):** `zetVluchtigeKey_`-chokepoint + cleanup-trigger + ban.~~ ✅ **GESLOTEN** — TTL-chokepoint + `VLUCHTIGE_PREFIXES`-registry + dagelijkse sweep; SUPPLETIE_GEMELD_ gemigreerd; ban-test eist chokepoint-of-geverifieerde-allowlist over src/ + licence-server/ en checkt dat élke geclaimde sweep echt bestaat + gewired is.
3. ~~**Klasse 4 (klant-mail-gate):** `stuurKlantNotificatie_`-chokepoint + ban.~~ ✅ **GESLOTEN** — chokepoint + 8 owner-notificaties gegate (lekke master-switch gefixt, F-MAIL-340) + `klant-mail-ok`-marker-ban.
4. ~~**Klasse 5 (copy↔code):** menupaden uit `Menu.gs`.~~ ✅ **GESLOTEN (mechanisch deel)** — `contract-menu-handlers` borgt dat élk menu-item een bestaande handler heeft; vrije-tekst claims = eerlijke grens (negatieve guards). Volledige menupad-uit-één-bron blijft optioneel structureel werk.
5. ~~**Klassen 6/8:** review-checklist hard maken.~~ ✅ **GESLOTEN** — `contract-privileged-auth` (6) + `contract-ledger-ratel` (8) maken de checklist mechanisch i.p.v. menselijk.
6. **Klasse 10 (grondslag-afbakening):** 0x90-contra GESLOTEN; software-01xx + €450-ondergrens = RB-grens (ledger F-TAX-331/332).
7. **Klasse 12 (loop-guillotine):** 3 sites migreren naar `guillotineCheck_`+cursor — volume-gated backlog (G1/G2/G3).

**Convergentie-ronde 2026-06-22 (claude/funny-ramanujan):** ronde startte met
9 klassen (3 PARTIAL: 5/6/8) + 6 blokker-assen. Resultaat: 5/6/8 → CLOSED;
2 lekken in "CLOSED"-klassen gerepareerd (klasse 2 bucket-split, klasse 3
variabele-vorm); 3 nieuwe klassen ontdekt+geboekt (10/11/12). Stand: **9 van 12
klassen 🟢 CLOSED; 10 CLOSED-met-RB-grens; 12 PARTIAL (volume-gated backlog).**

**Tweede convergentie-ronde (zelfde dag):** de 4 hercontrole-agents vonden 2
lekken in de EIGEN eerste-ronde-fixes — exact waarvoor de herhaling bestaat:
(a) F-RED-332 (HIGH): de test-modus-auto-revert miste de primaire dashboard-
toggle (`adminZetTestModus` ≠ de gefixte legacy-endpoint) → beide nu via één
chokepoint `_zetTestModusPreset_` + contract op élke €0,01-toggle; (b) F-TAX-334
(LAAG): de klasse-10-ban was file-scoped → 4e instantie in Notificaties ontsnapte
→ ban nu codebase-breed. Beide structureel gesloten (parallel-pad-drift +
opt-in-false-negative). De andere 2 agents (accountant, gas-runtime) bevestigden
"geen nieuwe klasse, geen regressie". Les: een fix is pas klaar als de
borging ÁLLE parallelle paden dekt — een file-scoped of single-endpoint-ratel
geeft vals vertrouwen (zelfde wortel als klasse 1's omgekeerde ban).

**Derde + vierde convergentie-ronde (zelfde dag):** de hercontrole-fixes werden
zelf hercontroleerd tot élke as schoon terugkwam. Ronde 3: red-team bevestigde
F-RED-332 compleet (schoon); tax vond F-TAX-335 (de klasse-10-ban was syntax-
scoped → MIA-`/^02[67]/` ontsnapte) → aparte chokepoint + ban syntax-volledig
gemaakt; red-team-scope-grens F-RED-333 (prijs-knop-€0,01) gesloten via stamp-on-
observe. Ronde 4 (tax, gericht): veegde álle evasie-vectoren (andere saldo-kolom,
charAt-prefix, FOR/afschrijving/reiskosten/lijfrente/8xxx-import) → **"klasse 10
leeg, convergentie bevestigd"**; de resterende theoretische varianten zijn
exclusion-gates, geen selectie-grondslagen. **Eindstand: élke as (accountant,
gas-runtime, red-team, tax) heeft een schone bevestigingsronde; elke as die een
lek vond is herdraaid tot schoon.** De les van rondes 2-4: een fix is pas klaar
als de borging álle dimensies dekt — bestand (file-scope), parallel pad
(single-endpoint), én syntaxvorm. Dit is convergentie in de enige eerlijke vorm.

Een mega-audit-herhaling is nu **zinvol-leeg voor de gesloten klassen**: hij kan
alleen nog een *nieuwe* klasse vinden of een instantie in 10/12 (beide bewust
geregistreerd met de RB-/structurele restwerk). **Dát** is "100% af" in de enige
eerlijke vorm: niet "nul bugs bewezen" (onmogelijk), maar "elke bekende
terugkerende klasse mechanisch gesloten of expliciet afgegrensd".

## Eerlijke grens
Dit register sluit *bekende* klassen. Een volledig nieuwe klasse (een soort bug
die we nog nooit zagen) kan nog steeds opduiken — daarvoor blijft één periodieke
audit nuttig, maar dan als *klasse-ontdekker*, niet als instantie-jager. En geen
enkele machine vervangt de jaarlijkse fiscale wetscheck of een echte klant.
