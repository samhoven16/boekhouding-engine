npx lint-staged

# website/version.json + website/changelog.json worden NIET meer hier
# bijgewerkt. Voorheen deed dit elke commit, wat bij elke PR een merge-
# conflict opleverde tussen main en branch. Beide files worden nu pas op
# push naar main bijgewerkt door .github/workflows/auto-update-version.yml.
# Lokaal kun je 'm handmatig draaien:
#   node scripts/update-version.js && node scripts/update-changelog.js
